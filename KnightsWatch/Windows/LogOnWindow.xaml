<Controls:MetroWindow x:Class="KnightsWatch.Windows.LogOnWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KnightsWatch.Windows"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:vm ="clr-namespace:KnightsWatch.Presentation.ViewModels;assembly=Presentation"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance vm:LogOnWindowViewModel}"
        Title="LogOnWindow" Height="300" Width="300" WindowStartupLocation="CenterScreen" >
    <Grid>
        <StackPanel>
            <TextBlock Padding="120,5,0,5">
                <Run Text="Welcome" />
            </TextBlock>
            <Button Command="{Binding LogOnCommandr4edc}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image/>
                            <TextBlock>Log On</TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </StackPanel>
    </Grid>
</Controls:MetroWindow>
